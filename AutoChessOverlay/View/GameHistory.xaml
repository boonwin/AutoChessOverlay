<Window x:Class="AutoChessOverlay.View.GameHistory"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        xmlns:local="clr-namespace:AutoChessOverlay.View"
        mc:Ignorable="d" 
        Title="Game History" Height="450" Width="525">

    <Grid Margin="10,10,2,0">
        <lvc:CartesianChart Series="{Binding SeriesCollection}" LegendLocation="Right" Margin="10,115,10,10" >
            <lvc:CartesianChart.AxisY>
                <lvc:Axis Title="Wins" LabelFormatter="{Binding YFormatter}"></lvc:Axis>
            </lvc:CartesianChart.AxisY>
            <lvc:CartesianChart.AxisX>
                <lvc:Axis Title="Time" Labels="{Binding Labels}"></lvc:Axis>
            </lvc:CartesianChart.AxisX>
        </lvc:CartesianChart>
        <DatePicker HorizontalAlignment="Left" Margin="10,10,0,0" VerticalAlignment="Top"/>
        <ComboBox Margin="158,10,0,0" VerticalAlignment="Top" HorizontalAlignment="Left" Width="120"/>
        <ComboBox HorizontalAlignment="Left" Margin="300,10,0,0" VerticalAlignment="Top" Width="120"/>
    </Grid>
</Window>